---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "models.fullname" . }}
  namespace: {{ .Values.namespace }}
  labels:
    app: vllm
    model: {{ .Values.configs.name }}
    name: {{ include "models.fullname" . }}
spec:
  ports:
  - name: http-vllm
    port: 80
    protocol: TCP
    targetPort: 8000
  {{- if .Values.configs.lmcache.enable }}
  - name: http-lmcache
    port: 7000
    protocol: TCP
    targetPort: 7000
  {{- end }}
  selector:
    app: vllm
    model: {{ .Values.configs.name }}
    name: {{ include "models.fullname" . }}
  sessionAffinity: None
  type: NodePort
